numbers = [10, 3, 55, 2, 44, 6]

max = numbers[0]
min = numbers [0]

for i in range(1,len(numbers)):
    if numbers[i] > max:
        max = numbers[i]
    if numbers[i] < min:
        min = numbers[i]

print(f'Max:{max}\nMin:{min}')
